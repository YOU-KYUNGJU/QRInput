# AGENTS.md

## 목표
이 저장소는 FITI 시험관리시스템 QR 입고 자동화를 `공통 엔진 + 팀별 설정` 구조로 운영하기 위한 저장소다.

## 우선 원칙
1. 팀별 차이는 반드시 `config/*.ini` 또는 `docs/team-profiles.md`로 분리한다.
2. 공통 엔진에서 `if team = ...` 같은 하드코딩 분기를 추가하지 않는다.
3. 좌표, 색상, 타임아웃, 경로, 계정, 실패 정책은 코드 상수로 박지 않는다.
4. 새 기능 추가 시 분석화학평가팀과 가공성능평가팀 둘 다 영향 범위를 검토한다.
5. 처리 성공 여부는 화면 결과만 믿지 말고 `history`와 `run log`에 남긴다.
6. UI 자동화는 운영 환경 표준(해상도, DPI, 창 위치)을 깨지 않는 방향으로 수정한다.
7. 운영 비밀번호는 저장소에 커밋하지 않는다.
8. 배포 전에는 최소한 `tests/fixtures` 기준 접수번호 생성 테스트와 처리이력 중복 방지 테스트를 통과해야 한다.

## Codex 작업 규칙
1. 작업 시작 전 반드시 다음 파일을 읽는다.
   - `AGENTS.md`
   - `docs/architecture.md`
   - `docs/team-profiles.md`
   - `docs/fiti-ui-contract.md`
   - `docs/logging-policy.md`
2. 설정 변경 요청이면 우선 `config/*.ini`를 수정하고, 공통 코드 변경은 마지막 수단으로 본다.
3. AHK 코드 수정 시 함수 단위로 분리한다.
4. 변경 시 다음 항목을 항상 답변에 포함한다.
   - 변경 파일
   - 변경 이유
   - 영향 받는 팀
   - 수동 테스트 방법
5. 실패 복구 로직, 재시도 로직, 이력 로그 키 구조를 깨는 변경은 금지한다.
6. 저장소에 민감정보가 들어 있으면 즉시 분리 방안을 제안한다.

## 금지 사항
1. 팀별 로그인 ID/PW 하드코딩
2. 성공/실패 이력 없이 파일 이동만으로 중복 방지 처리
3. 픽셀 좌표 하드코딩 추가
4. 재시도 횟수 무제한 처리
5. `Sleep`만 계속 늘리는 임시 대응
6. 팀 규칙 문서 미갱신 상태로 코드만 수정

## 기본 산출 형식
Codex는 코드 변경 요청에 대해 아래 순서로 답한다.
1. 변경 개요
2. 수정 파일 목록
3. 핵심 변경 포인트
4. 수동 테스트 절차
5. 남아 있는 리스크
